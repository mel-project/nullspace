# GUI quick start

This guide explains how to run the Nullspace GUI client locally.

## Prerequisites

- A working Rust toolchain (stable), including Cargo.

## Run the app

1. Clone this repository and `cd` into it.

2. Run the GUI client:

   ```sh
   cargo run -p nullspace-egui
   ```
